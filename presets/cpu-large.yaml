# cpu-large: Heavy CPU work (large datasets, parallel processing)
name: cpu-large
description: "Large CPU instance for compute-intensive workloads"

instance_type: c5.2xlarge
ami_pattern: "ubuntu/images/hvm-ssd/ubuntu-jammy-22.04-amd64-server-*"
ami_owner: "099720109477"
ami_id: ""

volume_size: 100
root_device: /dev/sda1
ssh_user: ubuntu
spot_friendly: true

cost_per_hour: 0.34
